{
  "private": true,
  "type": "module",
  "scripts": {
    "check:js": "node --check api/tts.js && node --check assets/js/fablea-ui.js && node --check assets/js/fablea-profile.js && node --check assets/js/fablea-grammar.js && node --check assets/js/fablea-story-engine.js",
    "check:css-classes": "node scripts/check-css-classes.mjs",
    "check:stories": "node scripts/check-story-quality.mjs",
    "check:smoke": "node scripts/functional-smoke.mjs",
    "test": "npm run check:js && npm run check:stories && npm run check:smoke && npm run check:css-classes"
  }
}
